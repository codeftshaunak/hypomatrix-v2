import AboutView from "@/views/about";
import React from "react";

type Props = {};

const about = (props: Props) => {
  return (
    <div>
      <AboutView />
    </div>
  );
};

export default about;
